/** Weekly Calling Card — sum of `xpEarned` in Sun–Sat week must reach this (see `callingCardWeek.ts`). */
export const WEEKLY_XP_TARGET = 280;

/** Bonus applied to each stat when a pledged week succeeds (on overlay dismiss). */
export const WEEKLY_BONUS_XP_PER_STAT = 40;

export const CALLING_CARD_PLEDGE_MAX_LEN = 500;
